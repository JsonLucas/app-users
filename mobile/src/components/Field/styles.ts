import { StyleSheet } from "react-native";

export const Styles = StyleSheet.create({
	Field: {
		padding: 10,
		width: '100%',
		border: 'none',
		borderRadius: 10,
		backgroundColor: 'white',
		color: 'black'
	}
});